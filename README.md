# QueryNest
QueryNest is a lightweight SQL quiz solver that transforms relational database questions into structured answers and generates submission-ready CSV files. It focuses on core SQL concepts such as SELECT, WHERE, JOIN, aggregation, and basic query interpretation — packaged in a clean Python workflow for academic submission.
